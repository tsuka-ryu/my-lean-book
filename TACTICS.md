# タクティク早見表

論理まわりの演習で迷ったとき用のメモ。

## 基本の考え方

1. **ゴールを分解して形を単純にする**（`intro` `constructor` `left` `right` `apply`）
2. **仮定を分解して使える形にする**（`cases` `.left` `.right`）
3. **ゴールと同じものが手元に現れたら終わり**（`exact` `assumption`）

証明は上から書き下すというより、**ゴール側と仮定側の両方から削って、真ん中で出会わせる**作業。

## ゴールの形で決める

| ゴールの形 | やること |
|---|---|
| `A → B`、`¬A` | `intro` |
| `A ∧ B`、`A ↔ B` | `constructor` |
| `A ∨ B` | `left` か `right` |
| `False` | 仮定の `¬X` と `X` をぶつける |
| 仮定と同じ形 | `exact` / `assumption` |
| 手に負えない | `exfalso` でゴールを `False` に差し替える |

## 作る側と使う側

| | ゴールにある（作る） | 仮定にある（使う） |
|---|---|---|
| `∧` | `constructor` | `h.left` / `h.right`（= `h.1` / `h.2`） |
| `∨` | `left` / `right`（= `Or.inl` / `Or.inr`） | `cases` |
| `↔` | `constructor` | `h.mp` / `h.mpr` |
| `→` | `intro` | `h x`（関数適用）/ `apply h` |
| `¬` | `intro`（`¬A` は `A → False`） | `h x` で `False` を作る |

## 覚えておくこと

- **`¬A` は `A → False` の別名**。`¬` を見たら矢印に戻して考える
- `False` が出てくるのは `¬` が絡むときだけ。`¬` のない命題なら登場しない
- **`apply` はゴールを「その関数に足りない材料」に置き換える**
- `intro` の名前は自由。型はゴールの形が決める
- 仮定は何度使ってもよいし、使わなくてもよい（未使用だと警告は出る）

## タクティク一覧

| タクティク | 意味 |
|---|---|
| `intro h` | ゴールの矢印の左を仮定 `h` にする |
| `exact h` | ゴールにぴったり合うものを指定して閉じる |
| `assumption` | ゴールに合う仮定を自動で探して閉じる |
| `apply h` | `h` の結論をゴールに合わせ、足りない前提を新ゴールにする |
| `constructor` | 「両方示せ」型のゴール（`∧` `↔`）を分割する |
| `cases h with` | 仮定の `∨` などを場合分けする |
| `left` / `right` | ゴール `A ∨ B` をどちらか一方に絞る |
| `exfalso` | ゴールを `False` に差し替える |
| `contradiction` | 矛盾する仮定を自動で探して閉じる |
| `trivial` | `True` など自明なゴールを閉じる |
| `rfl` | 両辺が同じものに簡約できることを示す |
| `tac1 <;> tac2` | `tac1` が作った全ゴールに `tac2` をかける |

## 記号の入力

`\` に続けて打ち、スペースか Tab で確定。

| 記号 | 入力 | 記号 | 入力 |
|---|---|---|---|
| `∀` | `\all` | `¬` | `\not` |
| `∃` | `\ex` | `∧` | `\and` |
| `→` | `\to` | `∨` | `\or` |
| `↔` | `\iff` | `·` | `\.` |
| `≤` `≥` | `\le` `\ge` | `⟨` `⟩` | `\<` `\>` |

記号にマウスを乗せると入力方法が表示される。一覧はコマンドパレットの
`Lean 4: Show Unicode Input Abbreviations`。
